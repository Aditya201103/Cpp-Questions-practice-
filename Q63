#include <iostream>
using namespace std;

class Demo {
    int x;
public:
    Demo() {
        x = 10;
    }
    friend void show(Demo d);
};

void show(Demo d) {
    cout << d.x;
}

int main() {
    Demo obj;
    show(obj);
    return 0;
}
