#include <iostream>
using namespace std;

class Stack {
    int a[5], top;
public:
    Stack() { top = -1; }

    void push(int x) {
        if (top == 4)
            cout << "Stack Overflow\n";
        else
            a[++top] = x;
    }

    void pop() {
        if (top == -1)
            cout << "Stack Underflow\n";
        else
            top--;
    }

    void display() {
        for (int i = top; i >= 0; i--)
            cout << a[i] << " ";
    }
};

int main() {
    Stack s;
    s.push(10);
    s.push(20);
    s.pop();
    s.display();
    return 0;
}
